These are standardized scripts/batch files which run tests or code reviews in the same way

_requirements.txt files are what extra pip requirements are required to run these


install with:
python -m pip install -U -r <requirements file>
